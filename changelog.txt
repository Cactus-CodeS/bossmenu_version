2.0.0 - 2026-03-03
------------------
  + Multi-Framework Support (VORP Core + RSG-Core)
  + Auto-detection bridge — detects framework at startup
  + Bridge abstraction layer (bridge/loader.lua, bridge/rsg/, bridge/vorp/)
  + All server/client calls go through Bridge.* API
  + Automatic job registration for RSG-Core (sync_jobs.lua)
  + Jobs from config.lua auto-synced to RSG-Core shared jobs at startup
  + /syncjobs command — manually re-sync all jobs to framework
  + /listjobs command — view all registered jobs in console
  + Server exports: SyncJobsViaExports(), JobExistsInCore()
  + Install helpers for RSG-Core (install/forBossmenu.lua, install/forMultijob.lua)
  + RSG-Core compatible notification system
  + RSG-Core compatible inventory integration
  + RSG-Core compatible paycheck system
  + Interaction modes: prompt, ox_target, pc_interaction
  + Notification modes: auto, rsg, vorp, redem, bln, nui
  * Avatar system now uses synchronous saves (no more race conditions)
  * Avatar no longer appears blank after saving
  * Affected rows validation on avatar save with error feedback
  * Permission checks are now type-safe (R() helper)
  * Configs using true/false instead of grade numbers no longer crash
  * ResolvePermValue() handles boolean-to-number conversion
  * All permission comparisons safe across sync_grades, grades, accounts, interaction
  * Improved SQL query performance
  * Updated fxmanifest.lua structure for bridge system
  * ~5000 lines of code (up from ~4500)
  ! Requires: oxmysql + (vorp_core OR rsg-core)
  ! ⚠️ Complete file replacement required — partial update will crash
